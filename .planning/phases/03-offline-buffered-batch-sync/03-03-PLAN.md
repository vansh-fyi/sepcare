---
phase: 03-offline-buffered-batch-sync
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/api/ingest/route.ts
  - tests/ingest.route.test.ts
autonomous: true
requirements: [ING-03]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "POST /api/ingest (single-reading route) treats a retried request with an identical deviceId+timestamp pair as a safe no-op — returns 201 without creating a duplicate row and without re-invoking computeAndPersistRiskScore for that no-op case (D-33/D-34 applied to the single-reading path, RESEARCH.md Pitfall 2)."
    - "A fresh (non-duplicate) POST to /api/ingest is unaffected by the upsert change — same 201 response contract and stored fields as before this plan."
  artifacts:
    - path: "src/app/api/ingest/route.ts"
      provides: ".upsert(row, {onConflict:'deviceId,timestamp', ignoreDuplicates:true}).select(...).maybeSingle() replacing the prior .insert(...).select(...).single() chain (Pitfall 2 fix)"
  key_links:
    - from: "src/app/api/ingest/route.ts"
      to: "readings table"
      via: ".upsert(row, {onConflict:'deviceId,timestamp', ignoreDuplicates:true}).maybeSingle()"
      pattern: "maybeSingle"
---

<objective>
Apply D-33's upsert-ignore-duplicates treatment to the existing single-reading `POST /api/ingest`
route (RESEARCH.md Pitfall 2) so both ingest paths share identical replay-safety semantics —
without this fix, a retried single-reading POST would start returning 500 instead of a safe 201
no-op once Plan 03-01's unique constraint lands, because `.single()` throws on the
now-legitimate zero-rows-returned case.

Purpose: D-33 explicitly names BOTH `POST /api/ingest` and `POST /api/ingest/batch` as requiring
upsert-ignore treatment — this plan closes the single-reading half of that decision, independent
of and parallel to Plan 03-02's batch-route work (no file overlap).
Output: `src/app/api/ingest/route.ts` updated to upsert-ignore semantics, with a passing
duplicate-retry regression test.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-offline-buffered-batch-sync/03-CONTEXT.md
@.planning/phases/03-offline-buffered-batch-sync/03-RESEARCH.md
@.planning/phases/03-offline-buffered-batch-sync/03-PATTERNS.md
@src/app/api/ingest/route.ts

<interfaces>
From Plan 03-01 (already on the live project by the time this plan executes): `readings` has a
unique constraint `readings_deviceid_timestamp_key` on `("deviceId","timestamp")`, usable as the
`onConflict` target this task's upsert call requires. This plan does not depend on Plan 03-02 and
touches no file Plan 03-02 touches (`src/app/api/ingest/route.ts` and
`tests/ingest.route.test.ts` only) — both plans run in the same wave.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply D-33's upsert-ignore fix to the single-reading route and prove duplicate-retry safety</name>
  <precondition>The readings_deviceid_timestamp_key unique constraint exists on the live Supabase
  project (Plan 03-01 Task 2 complete) — the onConflict target this task's upsert call
  requires.</precondition>
  <read_first>
    src/app/api/ingest/route.ts (current `.insert({...}).select(...).single()` chain, the exact
    lines to change); 03-RESEARCH.md Pitfall 2 (why `.single()` breaks once duplicates can be
    silently ignored, and the exact replacement code) and its "Modified single-reading route"
    Code Example; 03-PATTERNS.md's "src/app/api/ingest/route.ts (modified in place)" section;
    tests/ingest.route.test.ts (existing `describe`/`afterEach`/`makeRequest` shape to extend);
    03-CONTEXT.md D-33/D-34.
  </read_first>
  <files>src/app/api/ingest/route.ts, tests/ingest.route.test.ts</files>
  <behavior>
    - A first POST with a fresh deviceId+timestamp still returns 201 and stores the row exactly
      as before (regression — response contract unchanged).
    - A second, identical POST (same deviceId+timestamp+body) returns 201 (not 500), and exactly
      one row exists for that timestamp after both requests — no duplicate created.
    - The duplicate-retry case does not error when `computeAndPersistRiskScore` is skipped for
      the no-op reading (D-34) — the request still completes successfully end to end.
  </behavior>
  <action>
    In `src/app/api/ingest/route.ts`, replace the existing `.insert({ deviceId, timestamp,
    heartRate: vitals.heartRate, spo2: vitals.spo2, temperature: vitals.temperature,
    activityScore: vitals.activityScore }).select("id, deviceId, timestamp, heartRate,
    temperature, activityScore").single()` chain with `.upsert({ deviceId, timestamp, heartRate:
    vitals.heartRate, spo2: vitals.spo2, temperature: vitals.temperature, activityScore:
    vitals.activityScore }, { onConflict: "deviceId,timestamp", ignoreDuplicates: true
    }).select("id, deviceId, timestamp, heartRate, temperature, activityScore").maybeSingle()`.
    Change the failure check from `if (insertError || !insertedReading) { return 500 }` to `if
    (insertError) { return NextResponse.json({ error: "Failed to store reading" }, { status: 500
    }); }` alone (a `null` `insertedReading` with no `insertError` is now the expected
    duplicate-skip outcome, not a failure). Wrap the existing scoring block in `if
    (insertedReading) { try { await computeAndPersistRiskScore(insertedReading); } catch
    (scoringError) { console.error("Risk scoring failed for reading", insertedReading.id,
    scoringError); } }` so a duplicate-skip (`insertedReading` is `null`) falls through directly
    to `return NextResponse.json({ status: "ok" }, { status: 201 });` with scoring skipped
    (D-34). Extend `tests/ingest.route.test.ts` with one new `it(...)` block: POST a fresh
    payload with a unique timestamp, assert 201 and one stored row; POST the identical payload a
    second time, assert 201 again (not 500); then query `supabaseAdmin.from("readings").select("id").eq("timestamp",
    ts)` and assert exactly one row is returned.
  </action>
  <verify>
    <automated>npx vitest run tests/ingest.route.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the summary line</fails_when>
  </verify>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "failed" appears in the summary line (regression: tests/ingest.auth.test.ts, tests/risk.compute.test.ts, tests/realtime.*.test.ts must still pass — Phase 2's ON DELETE CASCADE cleanup path must not break)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/app/api/ingest/route.ts` uses `.upsert(..., {onConflict:"deviceId,timestamp",
      ignoreDuplicates:true})` and `.maybeSingle()`, not `.insert()`/`.single()`.
    - A fresh POST still returns 201 with the reading stored (no regression in response
      contract).
    - A duplicate retry POST returns 201, not 500, and does not create a second row for that
      timestamp.
    - `npm test` (full suite) passes.
  </acceptance_criteria>
  <done>The single-reading route safely no-ops on a retried duplicate insert, matching the batch
  route's D-33/D-34 semantics, proven by a passing vitest run with no regression in the existing
  suite.</done>
  <reversibility rating="reversible">This is a mechanical application of D-33 (already rated
  costly at the decision/constraint level in Plan 03-01) to this second call site — reverting this
  file alone is a simple diff back to plain `.insert()`/`.single()`; the costly-to-reverse piece is
  the live DB constraint itself, not this route's code.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ESP32 device -> POST /api/ingest (single) | Same trust boundary as Phase 1; re-examined here only for the upsert-ignore change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | readings unique constraint + upsert-ignore on the single-reading route (Task 1) | high | mitigate | Same D-33/D-34 mitigation as the batch route (Plan 03-02's T-03-04), applied to the second (single-reading) call site so both ingest paths share identical replay-safety semantics. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs this plan. |
</threat_model>

<verification>
`npx vitest run tests/ingest.route.test.ts` and `npm test` (full suite) both pass, proving a
retried single-reading POST is a safe 201 no-op (not a 500) with no regression to Phase 1/2's
existing behavior.
</verification>

<success_criteria>
Both ingest paths (`POST /api/ingest` and `POST /api/ingest/batch`) now share identical
upsert-ignore-duplicates replay-safety semantics against the same unique constraint (D-33),
proven by passing automated tests.
</success_criteria>

<output>
Create `.planning/phases/03-offline-buffered-batch-sync/03-03-SUMMARY.md` when done
</output>
