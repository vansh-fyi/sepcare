---
phase: 01-device-ingest-live-readout
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - .gitignore
  - tests/e2e-deployed.test.ts
autonomous: true
requirements: [ING-01, ING-02, STOR-01, READ-01, DEV-01]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Supabase Realtime subscription using the anon key delivers the latest stored vitals reading for the single provisioned device, with camelCase field names matching the ingest vocabulary — confirmed against the deployed Vercel URL, not just local dev."
    - "The deployed backend's /api/health route responds 200 with { status: 'ok' }, confirming the Vercel deployment and its wired Supabase env vars are live."
  artifacts:
    - path: "tests/e2e-deployed.test.ts"
      provides: "Production smoke test: real HTTP POST to the deployed URL, Realtime confirmation, cleanup"
  key_links:
    - from: "deployed Vercel production URL"
      to: "live Supabase project"
      via: "SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY / NEXT_PUBLIC_* env vars wired via `vercel env add`"
      pattern: "vercel env"
---

<objective>
Deploy the Next.js backend to Vercel's free tier with the Supabase runtime env vars wired in,
and prove the entire pipeline (device POST -> auth -> validate -> store -> Realtime read) works
against the actual deployed URL, not just local dev — the phase's real acceptance bar per
ROADMAP.md's success criteria.

Purpose: CLAUDE.md's hosting constraint requires a deployed (not just local) backend; RESEARCH.md's
Validation Architecture phase-gate sampling explicitly calls for one end-to-end check against the
deployed URL before the phase can be considered done.
Output: A live Vercel production deployment, confirmed reachable and functionally correct via an
automated deployed-environment smoke test.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-device-ingest-live-readout/01-CONTEXT.md
@.planning/phases/01-device-ingest-live-readout/01-RESEARCH.md
@.planning/phases/01-device-ingest-live-readout/01-VALIDATION.md

<interfaces>
From Plan 01: `GET /api/health` returns `{ status: 'ok' }`. From Plan 02: `.env.local` holds
`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL`,
`NEXT_PUBLIC_SUPABASE_ANON_KEY`, `DEVICE_API_KEY`. From Plan 03: `POST /api/ingest` is the real
route being exercised over HTTP in this plan (as opposed to Plan 03's in-process invocation), and
`tests/helpers/cleanup.ts` exports `deleteReadingByTimestamp`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to Vercel and wire the Supabase runtime env vars</name>
  <precondition>Plan 03's route handler and tests are committed and passing locally.</precondition>
  <read_first>
    gsd-core/references/checkpoints.md "Service CLI Reference" (Vercel row) and "Environment
    Variable Automation" (Vercel `env add` pattern); 01-RESEARCH.md "Open Questions #2" (no
    vercel.json needed).
  </read_first>
  <files>.gitignore</files>
  <action>
    Run `vercel --yes` in the repo root to create/link a Vercel project non-interactively. If this returns an authentication error, this is a dynamic auth gate — per the Authentication Gates protocol, halt this task, surface a `checkpoint:human-action` asking the user to run `vercel login`, then retry `vercel --yes` once authenticated (do not pre-plan this as a separate task; handle it exactly as the auth-gate pattern describes).
    Once linked, add the four Supabase runtime env vars from `.env.local` to both the `production` and `preview` scopes via `vercel env add SUPABASE_URL production`, `vercel env add SUPABASE_SERVICE_ROLE_KEY production`, `vercel env add NEXT_PUBLIC_SUPABASE_URL production`, `vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY production` (repeat each for `preview`), piping each value in from `.env.local` rather than retyping it and never printing the service-role value to the terminal.
    Confirm the Vercel CLI appended `.vercel` to `.gitignore` on first run; add it manually if it did not.
    Redeploy with `vercel --prod --yes` so the new env vars take effect; capture the resulting production URL for Task 2.
  </action>
  <verify>
    <automated>node -e "fetch(process.env.DEPLOYED_URL + '/api/health').then(r=>r.json()).then(j=>{console.log(JSON.stringify(j));process.exit(j.status==='ok'?0:1)}).catch(()=>process.exit(1))"</automated>
    <fails_when>non-zero exit code, a caught network error, or the printed JSON's status field is not "ok"</fails_when>
  </verify>
  <acceptance_criteria>
    - `vercel ls` shows a production deployment.
    - `vercel env ls production` and `vercel env ls preview` each list all four Supabase env vars.
    - `.gitignore` contains a line for `.vercel`.
    - The deployed `/api/health` route responds 200 with `{"status":"ok"}`.
  </acceptance_criteria>
  <done>Next.js backend deployed to Vercel (Hobby/free tier) with Supabase runtime env vars wired for production and preview; the health check confirms the deployment is live and correctly configured.</done>
</task>

<task type="auto">
  <name>Task 2: Deployed end-to-end smoke test — full pipeline against the production URL</name>
  <precondition>Task 1's production deployment is live and reachable; DEVICE_API_KEY (Plan 02) is available in .env.local.</precondition>
  <read_first>
    Plan 03 Task 2's `tests/realtime.subscribe.test.ts` (same assertions, but over real HTTP to
    the deployed URL instead of an in-process call); 01-VALIDATION.md's "Sampling Rate" phase-gate
    row (manual-or-scripted deployed check).
  </read_first>
  <files>tests/e2e-deployed.test.ts</files>
  <action>
    Write `tests/e2e-deployed.test.ts` (vitest), guarded to skip automatically unless `process.env.DEPLOYED_URL` is set (so it never runs as part of the regular local suite from Plans 01-03). When `DEPLOYED_URL` is set: subscribe an anon-key `supabase-js` client to `postgres_changes` on `readings` exactly as Plan 03 Task 2 did; perform a real `fetch(DEPLOYED_URL + '/api/ingest', { method: 'POST', headers: { 'X-API-Key': process.env.DEVICE_API_KEY }, body: JSON.stringify({ deviceId: 'nb-001', timestamp: <unique>, vitals: {...} }) })` against the LIVE deployed URL (a genuine HTTP round-trip, not an in-process call — this is what makes it a deployed-environment test rather than a repeat of Plan 03); assert the HTTP response status is 201; assert the anon subscriber receives the event within a 10-second timeout with the correct camelCase keys; delete the test row afterward via `deleteReadingByTimestamp` from `tests/helpers/cleanup.ts`.
    Run it once with `DEPLOYED_URL` set to the URL captured in Task 1.
  </action>
  <verify>
    <automated>DEPLOYED_URL="$DEPLOYED_URL" npx vitest run tests/e2e-deployed.test.ts</automated>
    <fails_when>non-zero exit code, the summary line contains "failed", the summary line reads "0 passed" or "1 skipped", or the HTTP response status captured in the test is not 201</fails_when>
  </verify>
  <acceptance_criteria>
    - A real HTTP POST to the deployed `/api/ingest` URL returns 201.
    - The anon Realtime subscriber receives the resulting event with correctly-cased camelCase keys.
    - The test row is deleted afterward — no residual data in the live table.
  </acceptance_criteria>
  <done>The full device-POST -> auth -> validate -> store -> Realtime-read pipeline is confirmed working against the actual deployed Vercel URL, not just local dev — the phase's real acceptance bar per ROADMAP's success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel dashboard/CLI env storage -> deployed function runtime | Production secrets (service-role key) live in Vercel's env store, injected at runtime only |
| Public internet -> deployed /api/ingest | The endpoint is now reachable by anyone, not just local dev — the same auth/RLS boundaries from Plan 03 are now internet-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-03 | Information Disclosure | Vercel env var wiring (Task 1) | critical | mitigate | Values piped in via `vercel env add` from `.env.local`, never typed/echoed into shell history or deploy logs; `NEXT_PUBLIC_*` vars are the only ones intentionally client-exposed, matching Pattern 2's isolation rule carried into production. |
| T-1-08 | Information Disclosure | Deployed build/runtime logs | high | mitigate | No task in this plan prints `SUPABASE_SERVICE_ROLE_KEY` or `SUPABASE_DB_PASSWORD` to stdout; only `/api/health`'s `{status:'ok'}` and the deployment URL are surfaced in verification output. |
| T-1-01, T-1-02 | Spoofing, Elevation of Privilege | Public internet exposure of /api/ingest and the Realtime channel | high | mitigate | Same mitigations as Plan 03 (T-1-01/T-1-02) now apply to a publicly-reachable endpoint; Task 2 re-proves both the auth boundary (implicitly, via the 201 requiring the real seeded key) and the RLS boundary against production. |
</threat_model>

<verification>
`vercel ls` shows a production deployment with all four env vars wired; the deployed `/api/health`
returns 200; `tests/e2e-deployed.test.ts` passes against the real production URL with no residual
test data left behind.
</verification>

<success_criteria>
The backend is deployed on Vercel's free tier and the full ingest -> store -> Realtime-read
pipeline is proven working against that real, public URL — ROADMAP.md Phase 1's success criteria
are met end-to-end, not just in local dev.
</success_criteria>

<operational_note>
RESEARCH.md Pitfall 3: Supabase free-tier projects auto-pause after 7 days of inactivity. This is
an ops note, not a code fix — record in this plan's SUMMARY that whoever owns the Supabase project
should check its dashboard status before a demo if there has been a week-plus gap since the last
ingest POST or Realtime subscription. Not blocking for this phase; no task changes as a result.
</operational_note>

<output>
Create `.planning/phases/01-device-ingest-live-readout/01-04-SUMMARY.md` when done
</output>
