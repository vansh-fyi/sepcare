---
phase: 01-device-ingest-live-readout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - .gitignore
  - .env.local.example
  - vitest.config.ts
  - src/app/api/health/route.ts
  - src/lib/supabase/admin.ts
  - src/lib/validation/ingest-schema.ts
  - supabase/config.toml
  - supabase/migrations/*_init.sql
autonomous: false
requirements: [STOR-01, DEV-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The readings table's DDL uses double-quoted camelCase identifiers so the Realtime wire payload matches the ingest vocabulary byte-for-byte (D-09, Pitfall 1)."
    - "The devices table has no anon SELECT policy — only the service-role client can query device credentials (D-04)."
  artifacts:
    - path: "supabase/migrations/*_init.sql"
      provides: "devices+readings schema, RLS policies, and Realtime publication (D-04, D-07, D-08, D-09)"
    - path: "src/lib/supabase/admin.ts"
      provides: "service-role Supabase client (Pattern 2 isolation)"
    - path: "src/lib/validation/ingest-schema.ts"
      provides: "zod IngestSchema for D-01's nested { deviceId, timestamp, vitals } payload"
    - path: "src/app/api/health/route.ts"
      provides: "deployment smoke-check endpoint used by Plan 04"
  key_links:
    - from: "src/app/api/ingest/route.ts (authored in Plan 03)"
      to: "src/lib/supabase/admin.ts"
      via: "import { supabaseAdmin } from '@/lib/supabase/admin'"
      pattern: "supabaseAdmin"
    - from: "supabase/migrations/*_init.sql"
      to: "live Supabase Postgres project"
      via: "supabase db push (Plan 02, Schema Push Detection Gate)"
      pattern: "db push"
---

<objective>
Scaffold the greenfield Next.js (App Router, TypeScript) backend, install the exact
researched dependency set, create the service-role Supabase client and ingest zod schema
modules, and author (but do not yet apply) the Supabase migration defining the `devices` and
`readings` tables with RLS and Realtime publication.

Purpose: This repo has zero prior code (01-PATTERNS.md confirms 0/6 analogs) — every
downstream plan (Supabase provisioning, the ingest route handler, deployment) depends on this
scaffold existing first.
Output: A buildable Next.js project with the two core library modules, a health-check route,
and a locally-authored (not-yet-pushed) SQL migration.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-device-ingest-live-readout/01-CONTEXT.md
@.planning/phases/01-device-ingest-live-readout/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project, install dependencies, create core library modules</name>
  <read_first>
    01-RESEARCH.md "Standard Stack" table (exact versions), "Installation" command block,
    "Pattern 2: Service-Role Client Isolation" (full code), the `IngestSchema` embedded in
    "Pattern 1" (lines ~183-192), and "Recommended Project Structure".
  </read_first>
  <files>package.json, tsconfig.json, next.config.ts, .gitignore, .env.local.example, vitest.config.ts, src/app/api/health/route.ts, src/lib/supabase/admin.ts, src/lib/validation/ingest-schema.ts</files>
  <action>
    Run `npx create-next-app@latest . --typescript --app --no-tailwind --eslint --src-dir --import-alias "@/*"` in the repo root, accepting non-interactive defaults for any remaining prompt.
    Run `npm install @supabase/supabase-js@2.116.0 zod@4.6.2` then `npm install -D vitest`.
    Create `src/lib/supabase/admin.ts` exporting a `supabaseAdmin` client built via `createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!, { auth: { persistSession: false } })` exactly per RESEARCH.md Pattern 2 — this module must never be imported by any file carrying a `"use client"` directive (Anti-Pattern: service-role key leaking into the client bundle).
    Create `src/lib/validation/ingest-schema.ts` exporting `IngestSchema`: a zod object with `deviceId: z.string().min(1)`, `timestamp: z.number().int()`, and a nested `vitals` object containing `heartRate`, `spo2`, `temperature`, `activityScore`, all `z.number()` — matching D-01's nested payload shape exactly.
    Add `src/app/api/health/route.ts` exporting a `GET` handler that returns `NextResponse.json({ status: 'ok' })` with HTTP 200 — used by Plan 04 as a deployment smoke-check.
    Create `.env.local.example` documenting the four runtime env vars with empty placeholder values (never real secrets): `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
    Confirm `.gitignore` (generated by `create-next-app`) already excludes `.env*.local`; if the line is absent, add it.
    Add `vitest.config.ts` using the Node test environment (no jsdom — this is a route-handler-only backend, no DOM), and add a `"test": "vitest run"` script to `package.json`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <fails_when>non-zero exit code, or the output contains "Failed to compile"</fails_when>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `next`, `react`, `react-dom`, `@supabase/supabase-js`, `zod` as `dependencies` and `vitest`, `typescript` as `devDependencies`.
    - `src/lib/supabase/admin.ts` exports `supabaseAdmin` constructed with `SUPABASE_SERVICE_ROLE_KEY`.
    - `src/lib/validation/ingest-schema.ts` exports `IngestSchema` matching the nested `{ deviceId, timestamp, vitals: { heartRate, spo2, temperature, activityScore } }` shape.
    - `src/app/api/health/route.ts` exists and exports a `GET` handler returning `{ status: 'ok' }`.
    - `.gitignore` contains a line excluding `.env*.local`.
    - `npm run build` completes with exit code 0.
  </acceptance_criteria>
  <done>Next.js TypeScript project scaffolded, core dependencies installed at the researched pinned versions, service-role client and ingest zod schema modules created, health-check route added, build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Confirm package legitimacy (next, @supabase/supabase-js, zod)</name>
  <what-built>package.json / package-lock.json now pin `next`, `@supabase/supabase-js`, and `zod` after scaffolding (Task 1).</what-built>
  <how-to-verify>
    1. Run `npm ls next @supabase/supabase-js zod` (or open `package-lock.json`) and confirm the resolved packages correspond to the official repos: `next` (github.com/vercel/next.js), `@supabase/supabase-js` (github.com/supabase/supabase-js), `zod` (github.com/colinhacks/zod).
    2. Confirm none of the three resolved to an unexpected or typosquatted name (e.g. not `nextjs`, `supabase-js`, `zod-validator`, or similar look-alikes).
    3. Context: RESEARCH.md's Package Legitimacy Audit flagged all three `[SUS]` under the "too-new" heuristic despite 20M-209M weekly downloads and long-lived official GitHub repos — this is one lightweight combined checkpoint per the researcher's explicit recommendation (not three separate ones), confirming the flag is a false positive before further work builds on these packages.
  </how-to-verify>
  <resume-signal>Type "approved" once confirmed, or name the package that looks wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author the Supabase migration (devices, readings, RLS, Realtime publication)</name>
  <read_first>
    01-RESEARCH.md "Code Examples -> Minimal devices + readings schema" (full DDL),
    "Pattern 3: RLS — Anon Read-Only, Service-Role Bypass", "Pitfall 1" (column casing),
    "Pitfall 2" (timestamp column type tradeoff), "Open Question 1" (device-id literal sync).
  </read_first>
  <reversibility rating="costly">Implements D-07 (direct Realtime read path) and D-08 (RLS boundary) — reversing to a REST-based read after the frontend is built against this Realtime contract would require rewriting the frontend's data-fetching layer (already flagged `costly` in CONTEXT.md).</reversibility>
  <files>supabase/config.toml, supabase/migrations/*_init.sql</files>
  <action>
    If the `supabase` CLI is not installed, install it via `npm install -g supabase`.
    Run `supabase init` in the repo root to scaffold `supabase/config.toml` and the `supabase/migrations/` directory, accepting non-interactive defaults.
    Run `supabase migration new init` to generate a correctly-timestamped empty migration file under `supabase/migrations/`.
    Populate that file with: a `devices` table (`device_id text primary key`, `api_key text not null`, `created_at timestamptz not null default now()`) with row level security enabled and NO policy granted to `anon` (service-role only, D-04); a `readings` table using double-quoted camelCase identifiers exactly `"deviceId" text not null references public.devices(device_id)`, `"timestamp" bigint not null` (epoch-ms integer per D-02 — bigint avoids the seconds/milliseconds ambiguity from Pitfall 2), `"heartRate" numeric not null`, `"spo2" numeric not null`, `"temperature" numeric not null`, `"activityScore" numeric not null`, plus `id bigint generated always as identity primary key` and `created_at timestamptz not null default now()` (quoted throughout so Postgres preserves the camelCase wire vocabulary, D-09, Pitfall 1); enable RLS on `readings` with a `select`-only policy named `"anon read-only single device"` granted `to anon` `using ("deviceId" = 'nb-001')` (D-08) — `nb-001` is the fixed device-id literal that Plan 02's seed step will insert with the SAME value, so the literal is defined exactly once here and reused, avoiding the two-place drift RESEARCH.md's Open Question 1 warned about. Finish with `alter publication supabase_realtime add table public.readings;` to enable Postgres Changes replication (D-07). Do NOT run `supabase db push` in this task — no live project is linked yet (that happens in Plan 02).
  </action>
  <verify>
    <automated>grep -c '"deviceId"' supabase/migrations/*_init.sql; grep -c 'alter publication supabase_realtime add table public.readings' supabase/migrations/*_init.sql; grep -c 'to anon' supabase/migrations/*_init.sql</automated>
    <fails_when>any of the three grep counts is 0, or no file matching supabase/migrations/*_init.sql exists</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one migration file exists under `supabase/migrations/` containing both `devices` and `readings` table definitions.
    - `readings` columns `"deviceId"`, `"timestamp"`, `"heartRate"`, `"spo2"`, `"temperature"`, `"activityScore"` are all double-quoted camelCase identifiers.
    - The RLS policy on `readings` filters on `"deviceId" = 'nb-001'` and grants `select` only `to anon`.
    - `devices` has row level security enabled with zero policies (no anon access of any kind).
    - The migration includes `alter publication supabase_realtime add table public.readings;`.
  </acceptance_criteria>
  <done>Migration authored locally with the full devices+readings schema, RLS policies, and Realtime publication — not yet applied to any live project (Plan 02 pushes it).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local dependency tree | Untrusted third-party code enters the project via `npm install` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-07 | Tampering | `package.json` / `package-lock.json` install targets (next, @supabase/supabase-js, zod) | high | mitigate | Task 2's `checkpoint:human-verify gate="blocking-human"` confirms all three resolve to their official GitHub-backed repos before any further code is written against them; never auto-approved even in yolo/auto-advance mode. |
</threat_model>

<verification>
`npm run build` succeeds; the migration file contains the exact schema/RLS/publication elements verified above. No live Supabase project exists yet — full end-to-end verification happens in Plan 03/04 once Plan 02 provisions and pushes.
</verification>

<success_criteria>
Repo is a buildable Next.js TypeScript App Router project with the researched dependency set,
the service-role client and ingest schema modules exist, and a correct-by-construction SQL
migration is authored and ready to push.
</success_criteria>

<output>
Create `.planning/phases/01-device-ingest-live-readout/01-01-SUMMARY.md` when done
</output>
