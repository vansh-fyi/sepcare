---
phase: 02-automatic-risk-scoring-status
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - tests/realtime.risk-scores.test.ts
  - tests/risk.compute.test.ts
autonomous: true
requirements: [RISK-02, STOR-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An anon-key Realtime subscriber receives a risk_scores INSERT event the moment POST /api/ingest computes and persists a status for nb-001, with no polling (mirrors READ-01's live-vitals wiring, D-20)."
    - "An anon-key Realtime subscriber does NOT receive a risk_scores event for a different device's reading (RLS boundary, mirrors Phase 1's T-1-02)."
    - "A risk_scores row is queryable filtered by its linked reading's timestamp range (STOR-02) - selecting risk_scores joined to readings WHERE readings.timestamp is between x and y returns exactly the expected rows, in timestamp order."
  artifacts:
    - path: "tests/realtime.risk-scores.test.ts"
      provides: "Automated proof of D-20/D-21's live-delivery and RLS-boundary wiring for risk_scores"
    - path: "tests/risk.compute.test.ts"
      provides: "Extended with a time-range join query proving STOR-02's queryability requirement"
  key_links:
    - from: "risk_scores table (Plan 02-01, supabase_realtime publication)"
      to: "anon Realtime subscriber (tests/realtime.risk-scores.test.ts)"
      via: "postgres_changes INSERT event on public.risk_scores, RLS-filtered"
      pattern: "postgres_changes"
    - from: "readings.timestamp"
      to: "risk_scores (via reading_id FK)"
      via: "PostgREST embedded-resource join: readings.select(\"timestamp, risk_scores(status, breakdown)\")"
      pattern: "risk_scores(status, breakdown)"
---

<objective>
Prove the two remaining goal-backward truths that only a live-wiring test (not a unit-style
`compute.ts` call) can establish: (1) the frontend's Realtime channel actually receives
`risk_scores` INSERT events, RLS-scoped exactly like `readings` (closing Phase 2's "read API
returns the current risk status alongside the latest vitals reading" success criterion via the
same Realtime path Phase 1 proved, D-20/D-21), and (2) `risk_scores` rows are genuinely
queryable by time range through their linked reading (STOR-02), across a realistic multi-reading
history.

Purpose: 02-VALIDATION.md flagged Realtime delivery as its one "manual-only" verification, but
Phase 1's own `tests/realtime.subscribe.test.ts` already proves this class of check is
automatable — this plan extends that proven pattern instead of leaving it manual.
Output: `tests/realtime.risk-scores.test.ts` (new) and an extended `tests/risk.compute.test.ts`
range-query assertion.
</objective>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-automatic-risk-scoring-status/02-CONTEXT.md
@.planning/phases/02-automatic-risk-scoring-status/02-VALIDATION.md
@tests/realtime.subscribe.test.ts
@tests/helpers/cleanup.ts

<interfaces>
From Plan 02-02 (already on disk by the time this plan executes): `computeAndPersistRiskScore`
exported from `src/lib/risk/compute.ts` (import path `@/lib/risk/compute`), and
`POST /api/ingest` now calls it synchronously after every successful `readings` insert. From
Plan 02-01: `risk_scores` is in the `supabase_realtime` publication with the same
`"deviceId" = 'nb-001'` anon RLS policy shape as `readings`, and `reading_id` is a declared
foreign key to `readings.id` (enabling a PostgREST embedded-resource join).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated Realtime proof for risk_scores INSERT + RLS boundary</name>
  <precondition>POST /api/ingest calls computeAndPersistRiskScore synchronously (Plan 02-02 Task
  1 complete) and risk_scores is in the supabase_realtime publication (Plan 02-01).</precondition>
  <read_first>
    tests/realtime.subscribe.test.ts in full (the exact openInsertSubscription pattern,
    DEVICE_ID/THROWAWAY_DEVICE_ID fixtures, and the deviceId+timestamp double-filter race-guard
    documented in its comments); 02-CONTEXT.md D-20/D-21.
  </read_first>
  <files>tests/realtime.risk-scores.test.ts</files>
  <action>
    Create `tests/realtime.risk-scores.test.ts` following `tests/realtime.subscribe.test.ts`'s
    exact structure: an `anonClient` built from `NEXT_PUBLIC_SUPABASE_URL`/
    `NEXT_PUBLIC_SUPABASE_ANON_KEY`, an `openInsertSubscription`-style helper subscribed to
    `postgres_changes` on `{event: "INSERT", schema: "public", table: "risk_scores"}` filtered by
    matching `reading_id` (not `deviceId`+`timestamp` — `risk_scores` rows carry no raw
    timestamp, so filter on the `reading_id` the POST's insert returns instead), resolving only
    after `SUBSCRIBED` status (same race-condition guard as the analog). Test 1: POST a valid
    nb-001 reading through the route handler, await the subscription's row, assert it matches the
    expected `status`/`breakdown` shape and that `Object.prototype.hasOwnProperty.call(row,
    "deviceid")` is `false` (camelCase preserved, mirrors the analog's Pitfall-1 check). Test 2:
    insert a throwaway non-nb-001 device + reading directly via `supabaseAdmin` (mirroring the
    analog's second test), call `computeAndPersistRiskScore` directly for that reading, and
    assert an anon subscriber filtered to that reading's id receives NO event within a 5s
    timeout (RLS boundary — the throwaway device's `risk_scores` row is invisible to `anon`).
  </action>
  <verify>
    <automated>npx vitest run tests/realtime.risk-scores.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - Test 1 receives a real risk_scores INSERT payload within its timeout and the payload's
      `status` field is one of `green`/`amber`/`red`.
    - Test 2's non-nb-001 subscriber receives `null` (no event) within its timeout, proving the
      RLS policy from Plan 02-01 actually blocks cross-device delivery.
    - No lowercase-folded key names (`deviceid`, `readingid`) appear on the received row.
  </acceptance_criteria>
  <done>A live anon-key Realtime subscriber demonstrably receives risk_scores INSERT events for
  nb-001 and is blocked from a different device's, closing out D-20/D-21's wiring with a real
  test instead of a manual step.</done>
</task>

<task type="auto">
  <name>Task 2: Prove risk_scores is queryable by time range through its linked reading (STOR-02)</name>
  <read_first>
    tests/risk.compute.test.ts (current state from Plan 02-02); 02-CONTEXT.md D-19 (no timestamp
    column on risk_scores — join-based range queries only).
  </read_first>
  <files>tests/risk.compute.test.ts</files>
  <action>
    Add one more test to `tests/risk.compute.test.ts`: insert a short synthetic history of 4
    `readings` for `nb-001` spaced roughly 2 hours apart (via direct `supabaseAdmin` inserts,
    each followed by a direct `computeAndPersistRiskScore` call so every reading has a linked
    `risk_scores` row), then query `supabaseAdmin.from("readings").select("timestamp,
    risk_scores(status, breakdown)").eq("deviceId", "nb-001").gte("timestamp", <the 2nd reading's
    timestamp>).lte("timestamp", <the 3rd reading's timestamp>).order("timestamp", {ascending:
    true})` (a PostgREST embedded-resource join, resolvable because `risk_scores.reading_id` is a
    declared foreign key to `readings.id`) and assert exactly the 2 in-range readings are
    returned, each carrying its own non-null nested `risk_scores` object, in ascending timestamp
    order — proving STOR-02's "queryable by time range" concretely rather than by inspection.
  </action>
  <verify>
    <automated>npx vitest run tests/risk.compute.test.ts</automated>
    <fails_when>non-zero exit, or "failed" appears in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - The range query returns exactly 2 rows (not 4, not 0) for the bounded window.
    - Each returned row's nested `risk_scores.status` is a non-null string.
    - Rows are ordered ascending by `timestamp`.
  </acceptance_criteria>
  <done>risk_scores is proven queryable by time range via its FK join to readings, closing
  STOR-02's final goal-backward truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon Realtime subscriber -> risk_scores changefeed | Same RLS-filtered boundary as readings; this plan's tests exercise both the allow and deny paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Realtime subscription test using anon key (Task 1) | low | accept | Test-only code path, mirrors Phase 1's proven realtime.subscribe.test.ts pattern; the anon key is already a public-safe credential (NEXT_PUBLIC_SUPABASE_ANON_KEY), not a secret. |
| T-02-01 (carried, verified) | Elevation of Privilege | risk_scores RLS boundary | low | mitigate | This plan's Task 1 adds the automated cross-device negative-delivery test proving the RLS policy from Plan 02-01 actually blocks a non-nb-001 device's risk_scores INSERT from reaching an anon subscriber. |
</threat_model>

<verification>
`npx vitest run tests/realtime.risk-scores.test.ts tests/risk.compute.test.ts` passes, proving
live Realtime delivery (with RLS boundary) and time-range queryability for risk_scores.
</verification>

<success_criteria>
A real anon-key subscriber receives risk_scores updates live and is blocked from other devices';
risk_scores is provably queryable by time range through its linked reading — closing out Phase
2's full success-criteria set (RISK-01 through RISK-03, STOR-02).
</success_criteria>

<output>
Create `.planning/phases/02-automatic-risk-scoring-status/02-03-SUMMARY.md` when done
</output>
