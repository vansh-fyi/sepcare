---
phase: 01-device-ingest-live-readout
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .env.local
  - supabase/config.toml
  - src/lib/supabase/types.ts
  - scripts/seed-device.mjs
  - scripts/check-device-seeded.mjs
autonomous: true
requirements: [DEV-01]

user_setup:
  - service: supabase
    why: "Postgres storage, RLS, and Realtime backing the vitals-ingest pipeline (STOR-01, READ-01); the account/project must exist before Claude can automate linking, schema push, and key retrieval via CLI."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens -> Generate new token (enables non-interactive `supabase login --token`, the CLI's documented non-TTY workaround)"
      - name: SUPABASE_DB_PASSWORD
        source: "Set when creating the project in Supabase Dashboard -> New Project (required non-interactively by `supabase db push`)"
    dashboard_config:
      - task: "Create a new Supabase project (free tier), e.g. named sepcare-backend"
        location: "https://supabase.com/dashboard -> New Project"

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Device provisioning (D-06) is a one-time manual/seed-script operation performed before the device is live, not a concurrent runtime code path — no concurrency guarantee is needed."
  prohibitions:
    - requirement_id: DEV-01
      category: safety
      verification: test
      statement: "MUST NOT seed the device with a weak/predictable api_key — it must be generated via a cryptographically secure random source with at least 256 bits of entropy"
    - requirement_id: DEV-01
      category: safety
      verification: test
      statement: "MUST NOT commit the generated device api_key, Supabase DB password, or service-role key into any git-tracked file"
  artifacts:
    - path: "src/lib/supabase/types.ts"
      provides: "generated Database row types for devices/readings, sourced from the live schema (not hand-typed)"
    - path: ".env.local"
      provides: "runtime secrets (gitignored): SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, DEVICE_API_KEY"
  key_links:
    - from: "supabase/migrations/*_init.sql (Plan 01)"
      to: "live Supabase Postgres schema"
      via: "supabase db push"
      pattern: "db push"
    - from: "devices table (seeded row, device_id='nb-001')"
      to: "src/app/api/ingest/route.ts auth check (Plan 03)"
      via: "device_id + api_key lookup"
      pattern: "nb-001"
---

<objective>
Link the local Supabase CLI config to the live Supabase project the developer created
(user_setup), push Plan 01's migration to that live project (the mandatory Schema Push
Detection Gate task), generate TypeScript row types from the live schema, and seed the single
v1 device row with a securely-generated API key.

Purpose: RESEARCH.md's Environment Availability table is explicit that "a live Supabase
project ... must be created before ingest/read success criteria can be verified. This is
inherent to the phase, not a gap." Nothing in Plan 03 can be verified against real data until
this plan completes.
Output: A live, linked Supabase project with the schema applied, generated types, and one
seeded device ready for Plan 03's ingest route handler to authenticate against.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `.env.local` — new runtime secrets file (gitignored): `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `DEVICE_API_KEY`
- `src/lib/supabase/types.ts` — exports generated `Database` row types for `devices`/`readings` (via `supabase gen types typescript --linked`)
- `scripts/seed-device.mjs` — new script: generates a 256-bit hex API key via `crypto.randomBytes(32)`, inserts the `nb-001` device row via `supabaseAdmin`, prints `DEVICE_API_KEY=<value>` once
- `scripts/check-device-seeded.mjs` — new script: verifies the `nb-001` device row exists, prints its `device_id` or exits non-zero with `missing`
- `supabase/config.toml` — modified: linked to the live Supabase project ref
</artifacts_produced>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-device-ingest-live-readout/01-CONTEXT.md
@.planning/phases/01-device-ingest-live-readout/01-RESEARCH.md

<interfaces>
From Plan 01 (already on disk): `src/lib/supabase/admin.ts` exports `supabaseAdmin` (service-role
client, constructed from `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`) — Task 2 below uses it
directly to seed the devices row. `supabase/migrations/*_init.sql` (Plan 01) defines `devices`
(`device_id`, `api_key`, `created_at`) and `readings` with the RLS literal `'nb-001'` already
baked in — Task 2 seeds `device_id = 'nb-001'` to match that exact literal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link the local Supabase CLI to the live project and capture runtime keys</name>
  <precondition>SUPABASE_ACCESS_TOKEN and SUPABASE_DB_PASSWORD are set, and a Supabase project has been created via the dashboard (user_setup dashboard_config step complete).</precondition>
  <read_first>
    01-RESEARCH.md "Environment Availability" table; gsd-core/references/checkpoints.md
    "Service CLI Reference" Supabase row (`init`, `link`, `db push`, `gen types`).
  </read_first>
  <files>.env.local, supabase/config.toml</files>
  <action>
    Run `supabase login --token "$SUPABASE_ACCESS_TOKEN"` to authenticate non-interactively.
    Run `supabase projects list --output json` and locate the project whose name matches the one created in the user_setup dashboard step (use the most-recently-created match if ambiguous); capture its id as `PROJECT_REF`.
    Run `supabase link --project-ref "$PROJECT_REF"` to link `supabase/config.toml` to that project.
    Run `supabase projects api-keys --project-ref "$PROJECT_REF" --output json` to fetch the `anon` and `service_role` keys.
    Construct `SUPABASE_URL` as `https://$PROJECT_REF.supabase.co`.
    Write `.env.local` (already excluded by `.gitignore` per Plan 01 Task 1) containing `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL` (same value as `SUPABASE_URL`), and `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
  </action>
  <verify>
    <automated>grep -cE '^(SUPABASE_URL|SUPABASE_SERVICE_ROLE_KEY|NEXT_PUBLIC_SUPABASE_URL|NEXT_PUBLIC_SUPABASE_ANON_KEY)=.+' .env.local</automated>
    <fails_when>the count is less than 4, or .env.local does not exist</fails_when>
  </verify>
  <acceptance_criteria>
    - `.env.local` contains all four keys, each with a non-empty value.
    - `supabase/config.toml` reflects a linked project (contains the project ref).
    - `git status --porcelain` shows `.env.local` as untracked/ignored, never staged.
  </acceptance_criteria>
  <done>Local Supabase CLI linked to the live project; runtime env vars captured in .env.local (gitignored).</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push schema, generate types, seed the v1 device</name>
  <precondition>.env.local from Task 1 exists with SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY populated, and SUPABASE_DB_PASSWORD is set (user_setup).</precondition>
  <read_first>
    This phase's Schema Push Detection Gate directive (build/type checks pass without a live
    schema — this task is what makes verification real, not a false positive); 01-RESEARCH.md
    "Code Examples" schema; CONTEXT.md D-06.
  </read_first>
  <files>src/lib/supabase/types.ts, scripts/seed-device.mjs, scripts/check-device-seeded.mjs</files>
  <action>
    Run `supabase db push --password "$SUPABASE_DB_PASSWORD"` to apply Plan 01's migration to the live project — this is the mandatory [BLOCKING] schema-push task; do not let Plan 03 proceed until this succeeds, since TypeScript/build checks alone would falsely pass without a live schema. If `supabase db push` fails due to a CLI-version or connectivity issue after one retry, fall back to applying the exact same migration SQL via the Supabase Dashboard's SQL Editor (paste the migration file's contents) and record which path was used in this plan's SUMMARY.
    After the push succeeds, run `supabase gen types typescript --linked > src/lib/supabase/types.ts` to generate row types for `devices`/`readings` directly from the live schema (avoids hand-typed drift from the DDL).
    Seed the single v1 device (D-06) by creating `scripts/seed-device.mjs`: it generates a 32-byte random hex API key via Node's `crypto.randomBytes(32).toString('hex')` (256 bits of entropy), imports `supabaseAdmin` from `src/lib/supabase/admin.ts`, inserts one row into `devices` (`device_id: 'nb-001'`, matching the RLS literal from Plan 01 Task 2; `api_key: <the generated key>`), prints the generated key to stdout exactly once clearly labelled (e.g. `DEVICE_API_KEY=<value>`) so it can be handed to the firmware/hardware track for flashing (D-06) — never written to any git-tracked file — and appends `DEVICE_API_KEY=<value>` to `.env.local` so Plan 03's tests can authenticate against the seeded device. Run it with `node scripts/seed-device.mjs`.
    Also create `scripts/check-device-seeded.mjs`, a small verification script that imports `supabaseAdmin`, selects `device_id` from `devices` where `device_id = 'nb-001'`, and prints the value (or `missing` with a non-zero exit) so the verify step below can confirm seeding succeeded.
  </action>
  <verify>
    <automated>supabase migration list --linked</automated>
    <fails_when>the init migration's name/timestamp is absent from the "Remote" column, or the command exits non-zero</fails_when>
  </verify>
  <verify>
    <automated>node scripts/check-device-seeded.mjs</automated>
    <fails_when>non-zero exit code, or the output does not contain the exact string "nb-001"</fails_when>
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` shows the init migration applied on the "Remote" side.
    - `src/lib/supabase/types.ts` exists and contains generated `Database` types referencing `devices` and `readings`.
    - Exactly one row exists in the live `devices` table with `device_id = 'nb-001'` and a 64-character hex `api_key`.
    - `.env.local` now also contains a non-empty `DEVICE_API_KEY`.
    - `git status --porcelain` shows no tracked file containing the generated API key, DB password, or service-role key.
  </acceptance_criteria>
  <done>Live Supabase schema pushed, TypeScript types generated from the live DB, and the single v1 device seeded with a securely-generated API key; the key is available in .env.local for Plan 03's tests and was printed once for the firmware track.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI/scripts -> live Supabase project | Service-role key, DB password, and access token cross this boundary and must never leave it via a git-tracked file or stdout beyond the one intentional device-key print |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-03 | Information Disclosure | `.env.local` write (Task 1), service-role key handling | critical | mitigate | Written only to `.env.local` (gitignored per Plan 01); never echoed to a committed file; `admin.ts` (Plan 01) is the only module that imports it. |
| T-1-09 | Spoofing | Seeded device `api_key` (Task 2) | high | mitigate | Generated via `crypto.randomBytes(32)` (256 bits of entropy) rather than a short/guessable value — satisfies prohibition P4 above. |
</threat_model>

<verification>
`supabase migration list --linked` shows the migration applied remotely; `node scripts/check-device-seeded.mjs` confirms the `nb-001` device row exists; `.env.local` carries all five runtime values (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `DEVICE_API_KEY`).
</verification>

<success_criteria>
The live Supabase project has the phase's full schema applied, generated TypeScript types exist, and the single v1 device is seeded and ready for Plan 03's ingest route handler to authenticate against.
</success_criteria>

<output>
Create `.planning/phases/01-device-ingest-live-readout/01-02-SUMMARY.md` when done
</output>
