---
phase: 02-automatic-risk-scoring-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/*_risk_scores.sql
  - src/lib/supabase/types.ts
autonomous: true
requirements: [STOR-02]

user_setup:
  - service: supabase
    why: "`supabase db push` needs the live Postgres DB password non-interactively. The project itself is already linked from Phase 1 (01-02-PLAN.md) — no new account, project, or access-token setup is needed here."
    env_vars:
      - name: SUPABASE_DB_PASSWORD
        source: "Same value used in Phase 1's 01-02-PLAN.md (originally: Supabase Dashboard -> New Project's DB password). Re-export it in the shell if the current session does not already have it set."

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "risk_scores.reading_id is itself the primary key (FK to readings.id, ON DELETE CASCADE) with no surrogate id column, making the reading<->score relationship a schema-level 1:1 guarantee (D-19)."
    - "risk_scores has RLS enabled with the identical anon-read-only-scoped-to-nb-001 policy shape as readings (D-21), and is added to the supabase_realtime publication (D-20)."
    - "readings gains a composite (\"deviceId\",\"timestamp\") B-tree index so every rolling-window query this phase introduces avoids a full sequential scan (RESEARCH.md Pitfall 1)."
  artifacts:
    - path: "supabase/migrations/*_risk_scores.sql"
      provides: "risk_scores table + composite readings index + RLS + Realtime publication (D-19 through D-22)"
    - path: "src/lib/supabase/types.ts"
      provides: "regenerated Database types including risk_scores Row/Insert/Update, sourced from the live schema"
  key_links:
    - from: "supabase/migrations/*_risk_scores.sql"
      to: "live Supabase Postgres schema"
      via: "supabase db push"
      pattern: "db push"
    - from: "risk_scores.reading_id"
      to: "readings.id"
      via: "foreign key, on delete cascade"
      pattern: "reading_id bigint primary key references public.readings(id) on delete cascade"
---

<objective>
Author the `risk_scores` migration (table, composite index on `readings`, RLS, Realtime
publication) per D-19 through D-22, and push it to the already-linked live Supabase project,
regenerating TypeScript types — the mandatory Schema Push Detection Gate for this phase.

Purpose: Nothing in Plan 02-02 (the compute engine) or Plan 02-03 (the Realtime/range-query
proof) can be verified against real data until a live `risk_scores` table and its generated
types exist — matching RESEARCH.md's own framing that this is inherent to the phase, not a gap.
Output: A live `risk_scores` table (RLS + Realtime), a composite index on `readings`, and
regenerated `src/lib/supabase/types.ts` ready for Plan 02-02 to build against.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `supabase/migrations/<new-timestamp>_risk_scores.sql` — new migration: `risk_scores` table
  (`reading_id` PK/FK-cascade, `"deviceId"`, `status` text+check, `breakdown` jsonb,
  `created_at`), RLS + anon read-only policy, Realtime publication membership, and a new
  `readings_deviceid_timestamp_idx` composite index on the existing `readings` table.
- `src/lib/supabase/types.ts` — regenerated (not hand-edited): adds `risk_scores` Row/Insert/Update
  types alongside the existing `devices`/`readings` types.
</artifacts_produced>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-automatic-risk-scoring-status/02-CONTEXT.md
@.planning/phases/02-automatic-risk-scoring-status/02-RESEARCH.md
@.planning/phases/02-automatic-risk-scoring-status/02-PATTERNS.md
@supabase/migrations/20260912172701_init.sql

<interfaces>
Phase 1 already established (unchanged by this plan): `devices(device_id, api_key, created_at)`,
`readings(id, "deviceId", "timestamp", "heartRate", "spo2", "temperature", "activityScore",
created_at)`, both RLS-enabled, `readings` in the `supabase_realtime` publication, and the literal
`'nb-001'` as the single provisioned device's RLS-policy scope. This plan extends that schema
additively — it does not modify `devices` or `readings`' existing columns, only adds an index to
`readings` and a brand-new `risk_scores` table.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the risk_scores migration</name>
  <read_first>
    supabase/migrations/20260912172701_init.sql (exact RLS/Realtime/quoted-camelCase conventions
    to mirror); 02-RESEARCH.md "Code Examples" risk_scores migration + Pitfall 1 (missing index)
    and Pitfall 3 (ON DELETE CASCADE is load-bearing for existing test cleanup) and Pitfall 4
    (publication membership must be hand-written); 02-CONTEXT.md D-19 through D-22.
  </read_first>
  <files>supabase/migrations/*_risk_scores.sql</files>
  <action>
    Run `supabase migration new risk_scores` to generate a correctly-timestamped empty file under
    `supabase/migrations/`. Populate it with exactly five statements, in order: (1)
    `create table public.risk_scores` with `reading_id bigint primary key references
    public.readings(id) on delete cascade` (D-19's 1:1-as-PK — deliberately NO separate surrogate
    `id` column, per RESEARCH.md's Primary recommendation), `"deviceId" text not null references
    public.devices(device_id)` (denormalized so the RLS policy stays a literal comparison,
    matching `readings`), `status text not null check (status in ('green', 'amber', 'red'))`,
    `breakdown jsonb not null`, `created_at timestamptz not null default now()`; (2)
    `alter table public.risk_scores enable row level security;`; (3) a `select`-only policy named
    exactly `"anon read-only single device"` `to anon` `using ("deviceId" = 'nb-001')` — the
    identical literal and policy name Phase 1 used for `readings` (D-21); (4)
    `alter publication supabase_realtime add table public.risk_scores;` (D-20); (5)
    `create index readings_deviceid_timestamp_idx on public.readings ("deviceId", "timestamp");`
    — a composite index on the EXISTING `readings` table (equality column `"deviceId"` first,
    range column `"timestamp"` second), closing RESEARCH.md's Pitfall 1 gap that every window
    query in Plan 02-02/02-03 depends on. Do NOT run `supabase db push` in this task — that is
    Task 2's dedicated blocking step.
  </action>
  <verify>
    <automated>grep -c 'on delete cascade' supabase/migrations/*_risk_scores.sql; grep -c 'alter publication supabase_realtime add table public.risk_scores' supabase/migrations/*_risk_scores.sql; grep -c 'readings_deviceid_timestamp_idx' supabase/migrations/*_risk_scores.sql</automated>
    <fails_when>any of the three printed counts is 0, or no file matches supabase/migrations/*_risk_scores.sql</fails_when>
  </verify>
  <acceptance_criteria>
    - The new migration file exists at `supabase/migrations/<ts>_risk_scores.sql` (matches glob
      `*_risk_scores.sql`).
    - File contains `reading_id bigint primary key references public.readings(id) on delete
      cascade` (no separate surrogate `id` column on `risk_scores`).
    - File contains the exact policy name `"anon read-only single device"` scoped to
      `"deviceId" = 'nb-001'`.
    - File contains `alter publication supabase_realtime add table public.risk_scores;`.
    - File contains `create index readings_deviceid_timestamp_idx on public.readings
      ("deviceId", "timestamp");`.
    - `supabase db push` has NOT been run yet — no live schema change from this task.
  </acceptance_criteria>
  <done>The risk_scores migration file is authored on disk with schema, RLS, Realtime publication,
  and the readings composite index — not yet applied to the live project.</done>
  <reversibility rating="costly">Once Plan 02-02's compute engine and Phase 3/4's batch-scoring
  and trend queries depend on this exact schema shape (reading_id as PK, status text+check,
  breakdown jsonb), changing the shape later requires migrating every dependent consumer —
  cheap to get right now, coordinated to change later.</reversibility>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push schema to live Supabase and regenerate types</name>
  <precondition>supabase/config.toml is already linked to the live project (from Phase 1's
  01-02-PLAN.md) and SUPABASE_DB_PASSWORD is set in the environment.</precondition>
  <read_first>
    This phase's Schema Push Detection Gate directive (build/type checks pass without a live
    schema — this task is what makes verification real, not a false positive); 01-02-PLAN.md
    Task 2's proven `supabase db push --password` / `gen types` sequence; RESEARCH.md Pitfall 4.
  </read_first>
  <files>src/lib/supabase/types.ts</files>
  <action>
    Run `supabase db push --password "$SUPABASE_DB_PASSWORD"` to apply Task 1's migration to the
    live project — this is the mandatory [BLOCKING] schema-push task; no later plan may proceed
    until this succeeds, since TypeScript/build checks alone pass without a live risk_scores
    table (a false-positive verification risk). If `supabase db push` fails due to a CLI-version
    or connectivity issue after one retry, fall back to pasting the exact migration SQL into the
    Supabase Dashboard's SQL Editor and record which path was used in this plan's SUMMARY, exactly
    as 01-02-PLAN.md's Task 2 documented. After the push succeeds, run `supabase gen types
    typescript --linked > src/lib/supabase/types.ts` to regenerate row types for
    devices/readings/risk_scores directly from the live schema.
  </action>
  <verify>
    <automated>supabase migration list --linked</automated>
    <fails_when>the risk_scores migration's timestamp/name is absent from the "Remote" column, or the command exits non-zero</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'risk_scores' src/lib/supabase/types.ts</automated>
    <fails_when>the printed count is 0, or src/lib/supabase/types.ts does not exist</fails_when>
  </verify>
  <verify>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or the build output contains a TypeScript error</fails_when>
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` shows the risk_scores migration applied on the Remote
      side.
    - `src/lib/supabase/types.ts` contains generated Row/Insert/Update types for `risk_scores`
      (reading_id, deviceId, status, breakdown, created_at).
    - `npm run build` passes with the regenerated types (no type drift breaking existing
      devices/readings usage).
    - No tracked file contains SUPABASE_DB_PASSWORD's value.
  </acceptance_criteria>
  <done>The live Supabase project has risk_scores + the readings composite index applied, and
  src/lib/supabase/types.ts reflects the new schema — Plan 02-02 can now write against real
  generated types and a real live table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI -> live Supabase project | `SUPABASE_DB_PASSWORD` crosses this boundary for `supabase db push`; must never leave it via a git-tracked file or verification-output echo |
| anon key -> risk_scores table | New RLS-scoped read boundary, identical shape to Phase 1's `readings` boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | `risk_scores` anon RLS policy (Task 1) | low | mitigate | Policy scoped to `"deviceId" = 'nb-001'`, the identical proven shape as `readings`' policy (D-21) — only matters once multi-device exists (out of v1 scope) but implemented now at zero extra cost. |
| T-02-02 | Information Disclosure | `SUPABASE_DB_PASSWORD` used for `supabase db push` (Task 2) | high | mitigate | Read from the shell environment only, same handling as Phase 1's T-1-03 — never written to a git-tracked file or echoed in verification output. |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new npm/pip/cargo dependency is introduced by this plan — RESEARCH.md's Package Legitimacy Audit is not applicable (zero new packages this phase). |
</threat_model>

<verification>
`supabase migration list --linked` shows the risk_scores migration applied remotely;
`src/lib/supabase/types.ts` contains generated `risk_scores` types; `npm run build` passes.
</verification>

<success_criteria>
The live Supabase project has the risk_scores table, its RLS policy, its Realtime publication
membership, and the readings composite index applied; generated TypeScript types exist for
Plan 02-02 to build against.
</success_criteria>

<output>
Create `.planning/phases/02-automatic-risk-scoring-status/02-01-SUMMARY.md` when done
</output>
