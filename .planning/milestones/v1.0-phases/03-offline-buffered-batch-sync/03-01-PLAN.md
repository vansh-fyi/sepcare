---
phase: 03-offline-buffered-batch-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/*_readings_unique_device_timestamp.sql
  - src/lib/supabase/types.ts
autonomous: true
requirements: [ING-03]

user_setup:
  - service: supabase
    why: "`supabase db push` needs the live Postgres DB password non-interactively. The project is already linked from Phase 1/2 — no new account, project, or access-token setup is needed here."
    env_vars:
      - name: SUPABASE_DB_PASSWORD
        source: "Same value used in Phase 1's 01-02-PLAN.md and Phase 2's 02-01-PLAN.md live-push flow. Re-export it in the shell if the current session does not already have it set."

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "readings has a new unique constraint on (\"deviceId\",\"timestamp\") enforced at the DB level, so a retried insert with an identical device+timestamp pair can be rejected via ON CONFLICT DO NOTHING instead of creating a duplicate row (D-33)."
    - "Any pre-existing exact-duplicate (deviceId,timestamp) rows are removed before the unique constraint is added, since ALTER TABLE ... ADD CONSTRAINT UNIQUE fails immediately (no NOT VALID option) if any existing row violates it."
  artifacts:
    - path: "supabase/migrations/*_readings_unique_device_timestamp.sql"
      provides: "Unique constraint on readings(\"deviceId\",\"timestamp\") plus a defensive pre-dedup delete statement (D-33)"
    - path: "src/lib/supabase/types.ts"
      provides: "Regenerated Database types confirming the live schema push landed cleanly (no Row/Insert/Update shape change expected — a constraint, not a new column)"
  key_links:
    - from: "supabase/migrations/*_readings_unique_device_timestamp.sql"
      to: "live Supabase Postgres schema"
      via: "supabase db push"
      pattern: "db push"
    - from: "readings(\"deviceId\",\"timestamp\") unique constraint"
      to: "POST /api/ingest and POST /api/ingest/batch upsert calls (Plans 03-02/03-03)"
      via: "onConflict: \"deviceId,timestamp\", ignoreDuplicates: true"
      pattern: "onConflict"
---

<objective>
Author the migration that adds a unique constraint on `readings("deviceId","timestamp")` (D-33)
and push it to the already-linked live Supabase project, regenerating TypeScript types — the
mandatory Schema Push Detection Gate for this phase. Both `POST /api/ingest` (Plan 03-03) and the
new `POST /api/ingest/batch` (Plan 03-02) depend on this constraint existing as the `onConflict`
target for their upsert-ignore-duplicates calls.

Purpose: Neither Plan 03-02's batch endpoint nor Plan 03-03's single-route retrofit can be tested
against real duplicate-retry behavior until this constraint exists live — build/type checks alone
pass without it, which is a false-positive verification risk this phase's Schema Push Detection
Gate directive explicitly flags.
Output: A live `readings("deviceId","timestamp")` unique constraint and regenerated
`src/lib/supabase/types.ts`, ready for Plans 03-02/03-03 to build against.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `supabase/migrations/<new-timestamp>_readings_unique_device_timestamp.sql` — new migration: a
  defensive pre-dedup `delete` statement followed by
  `alter table public.readings add constraint readings_deviceid_timestamp_key unique
  ("deviceId", "timestamp");`.
- `src/lib/supabase/types.ts` — regenerated (not hand-edited) after the push.
</artifacts_produced>

<execution_context>
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/workflows/execute-plan.md
@/Users/hp/Desktop/Work/Repositories/sepcare/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-offline-buffered-batch-sync/03-CONTEXT.md
@.planning/phases/03-offline-buffered-batch-sync/03-RESEARCH.md
@.planning/phases/03-offline-buffered-batch-sync/03-PATTERNS.md
@supabase/migrations/20260912172701_init.sql
@supabase/migrations/20260918102702_risk_scores.sql

<interfaces>
Existing (unchanged by this plan): `readings(id, "deviceId", "timestamp", "heartRate", "spo2",
"temperature", "activityScore", created_at)` with a plain (non-unique) composite index
`readings_deviceid_timestamp_idx` on `("deviceId","timestamp")` from Phase 2. This plan adds a
UNIQUE constraint on the same two columns — additive, does not replace or drop the existing plain
index (leaving both is safe at this project's demo scale per RESEARCH.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the unique-constraint migration</name>
  <read_first>
    supabase/migrations/20260912172701_init.sql (existing readings DDL, exact quoted-camelCase
    convention to mirror); supabase/migrations/20260918102702_risk_scores.sql (most recent
    migration — sets the sibling composite-index convention this constraint's implicit index
    overlaps with); 03-RESEARCH.md "Code Examples: Migration: unique constraint with defensive
    pre-dedup" section (exact DELETE + ALTER TABLE syntax and rationale); 03-CONTEXT.md D-33.
  </read_first>
  <files>supabase/migrations/*_readings_unique_device_timestamp.sql</files>
  <action>
    Run `supabase migration new readings_unique_device_timestamp` to generate a
    correctly-timestamped empty file under `supabase/migrations/`. Populate it with exactly two
    statements, in order: (1) a defensive delete removing any pre-existing exact duplicate
    `("deviceId","timestamp")` rows, keeping the lower-`id` (earliest-inserted) row deterministically:
    `delete from public.readings a using public.readings b where a.id > b.id and a."deviceId" =
    b."deviceId" and a."timestamp" = b."timestamp";` — required because `ALTER TABLE ... ADD
    CONSTRAINT ... UNIQUE` has no `NOT VALID` escape hatch and fails immediately if any existing
    rows violate it; (2) `alter table public.readings add constraint
    readings_deviceid_timestamp_key unique ("deviceId", "timestamp");`. Do NOT drop the existing
    `readings_deviceid_timestamp_idx` plain index from the risk_scores migration — leave it in
    place; the new constraint's implicit index is additive, dropping the old one is out of this
    phase's scope. Do NOT run `supabase db push` in this task — that is Task 2's dedicated
    blocking step.
  </action>
  <verify>
    <automated>grep -c 'add constraint readings_deviceid_timestamp_key unique' supabase/migrations/*_readings_unique_device_timestamp.sql; grep -c 'delete from public.readings a' supabase/migrations/*_readings_unique_device_timestamp.sql</automated>
    <fails_when>either printed count is 0, or no file matches supabase/migrations/*_readings_unique_device_timestamp.sql</fails_when>
  </verify>
  <acceptance_criteria>
    - The new migration file exists at `supabase/migrations/<ts>_readings_unique_device_timestamp.sql`.
    - File contains `add constraint readings_deviceid_timestamp_key unique ("deviceId", "timestamp")`.
    - File contains the defensive pre-dedup `delete from public.readings a using public.readings b ...` statement, ordered BEFORE the `alter table` statement.
    - The existing `readings_deviceid_timestamp_idx` index (from the risk_scores migration) is NOT dropped by this file.
    - `supabase db push` has NOT been run yet — no live schema change from this task.
  </acceptance_criteria>
  <done>The unique-constraint migration file is authored on disk with the defensive dedup delete
  and the constraint — not yet applied to the live project.</done>
  <reversibility rating="costly">Once Plan 03-02's batch route and Plan 03-03's single-route
  retrofit both rely on `onConflict: "deviceId,timestamp"` upsert-ignore against this exact
  constraint, removing it later requires reverting both routes to plain inserts and manually
  cleaning up any duplicate rows created in the interim.</reversibility>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push schema to live Supabase and regenerate types</name>
  <precondition>supabase/config.toml is already linked to the live project (from Phase 1/2) and
  SUPABASE_DB_PASSWORD is set in the environment.</precondition>
  <read_first>
    This phase's Schema Push Detection Gate directive (build/type checks pass without a live
    schema change — this task is what makes verification real, not a false positive);
    02-01-PLAN.md Task 2's proven `supabase db push --password` / `gen types` sequence (reuse
    verbatim); 03-RESEARCH.md Pitfall 4 (unrelated to this push directly, but confirms the
    project's Vercel/Fluid-Compute assumptions this phase relies on downstream).
  </read_first>
  <files>src/lib/supabase/types.ts</files>
  <action>
    Run `supabase db push --password "$SUPABASE_DB_PASSWORD"` to apply Task 1's migration to the
    live project — this is the mandatory [BLOCKING] schema-push gate; no later plan (03-02, 03-03)
    may run a live-database test until this succeeds, since TypeScript/build checks alone pass
    without the live constraint (a false-positive verification risk). If it fails due to a
    CLI-version or connectivity issue after one retry, fall back to pasting the migration SQL into
    the Supabase Dashboard's SQL Editor and record which path was used in this plan's SUMMARY,
    exactly as 01-02-PLAN.md's and 02-01-PLAN.md's Task 2 documented. After the push succeeds, run
    `supabase gen types typescript --linked > src/lib/supabase/types.ts` to regenerate types from
    the live schema.
  </action>
  <verify>
    <automated>supabase migration list --linked</automated>
    <fails_when>the readings_unique_device_timestamp migration's timestamp/name is absent from the "Remote" column, or the command exits non-zero</fails_when>
  </verify>
  <verify>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or the build output contains a TypeScript error</fails_when>
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` shows the readings_unique_device_timestamp migration
      applied on the Remote side.
    - `npm run build` passes with the regenerated types (no type drift breaking existing
      devices/readings/risk_scores usage).
    - No tracked file contains SUPABASE_DB_PASSWORD's value.
  </acceptance_criteria>
  <done>The live Supabase project has the unique constraint on readings("deviceId","timestamp")
  applied, and src/lib/supabase/types.ts is regenerated — Plans 03-02/03-03 can now test real
  duplicate-retry behavior against a live constraint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI -> live Supabase project | `SUPABASE_DB_PASSWORD` crosses this boundary for `supabase db push`; must never leave it via a git-tracked file or verification-output echo |
| Migration's pre-dedup delete -> readings table | A DDL-adjacent DML statement that silently removes rows before the constraint lands; must be deterministic and auditable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `SUPABASE_DB_PASSWORD` used for `supabase db push` (Task 2) | high | mitigate | Read from the shell environment only, same handling as Phase 1/2's proven pattern — never written to a git-tracked file or echoed in verification output. |
| T-03-02 | Tampering | Defensive pre-dedup `delete` in the migration (Task 1) | medium | mitigate | Deletion targets ONLY exact `("deviceId","timestamp")` duplicates and deterministically keeps the lower-`id` (earliest-inserted) row, never the newest — the exact statement is committed to the tracked migration file for auditability, not run ad hoc. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new npm/pip/cargo dependency is introduced by this plan — RESEARCH.md's Package Legitimacy Audit is not applicable (zero new packages this phase). |
</threat_model>

<verification>
`supabase migration list --linked` shows the readings_unique_device_timestamp migration applied
remotely; `npm run build` passes with regenerated types.
</verification>

<success_criteria>
The live Supabase project has a unique constraint on `readings("deviceId","timestamp")`, with no
pre-existing duplicate rows violating it; regenerated TypeScript types exist for Plans 03-02/03-03
to build against.
</success_criteria>

<output>
Create `.planning/phases/03-offline-buffered-batch-sync/03-01-SUMMARY.md` when done
</output>
