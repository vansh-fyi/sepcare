---
milestone: 1.0
audited: 2026-09-20T00:00:00Z
status: tech_debt
scores:
  requirements: 11/11
  phases: 4/4
  integration: 4/4
  flows: 4/4
gaps: {}
tech_debt:
  - phase: 01-device-ingest-live-readout
    items:
      - "ROADMAP.md marks this phase Mode: mvp but the phase goal isn't in user-story format — flagged, non-blocking, carried forward across two verification passes (no UI in this backend-only branch)."
  - phase: 03-offline-buffered-batch-sync
    items:
      - "D-29 batch scoring call order (ascending timestamp) is unverified by a dedicated test — code-inspection only, declared human_judgment: true in 03-02-SUMMARY.md."
      - "D-29 scoring-order correctness backstop (final risk_scores invariant to internal scoring order) is unverified by a dedicated test — declared verification: backstop in 03-02-PLAN.md."
  - phase: cross-phase (integration checker)
    items:
      - "GET /api/readings has no API-key/auth gate — read access is protected only by the hardcoded nb-001 device allow-list. Acceptable for v1 single-device scope (DEV-01) but must be revisited before the dashboard/API is exposed beyond the current trusted network."
  - phase: all (Nyquist)
    items:
      - "All 4 phases' VALIDATION.md files are status: draft (wave_0_complete: false) — validate-phase was never run to reconcile Nyquist compliance. This is a coverage TODO, not a proven compliance failure (see Nyquist Compliance section)."
nyquist:
  compliant_phases: 0
  partial_phases: 0
  not_validated_phases: 4
  missing_phases: 0
  overall: not_validated
---

# Milestone v1.0 Audit: SepCare Backend

**Audited:** 2026-09-20
**Status:** ⚡ tech_debt — all requirements met, no critical blockers, accumulated debt should be reviewed before/at close

## Scope

Phases 1–4 (all phases in the project). This is the project's first milestone.

## Requirements Coverage (3-Source Cross-Reference)

All 11 v1 requirements cross-checked against REQUIREMENTS.md traceability, each phase's VERIFICATION.md requirements evidence, and each plan's SUMMARY.md `requirements-completed` frontmatter.

| Requirement | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|---|---|---|---|---|---|
| ING-01 | 1 | passed | 01-03, 01-04 | [x] | **satisfied** |
| ING-02 | 1 | passed | 01-03, 01-04 | [x] | **satisfied** |
| ING-03 | 3 | passed | 03-01, 03-02, 03-03 | [x] | **satisfied** |
| STOR-01 | 1 | passed | 01-01, 01-03, 01-04 | [x] | **satisfied** |
| STOR-02 | 2 | passed | 02-01, 02-03 | [x] | **satisfied** |
| RISK-01 | 2 | passed | 02-02 | [x] | **satisfied** |
| RISK-02 | 2 | passed | 02-02, 02-03 | [x] | **satisfied** |
| RISK-03 | 2 | passed | 02-02 | [x] | **satisfied** |
| READ-01 | 1 | passed | 01-03, 01-04 | [x] | **satisfied** |
| READ-02 | 4 | passed | 04-01 | [x] | **satisfied** |
| DEV-01 | 1 | passed | 01-01, 01-02, 01-03, 01-04 | [x] | **satisfied** |

**Coverage: 11/11 satisfied. No orphaned requirements** — every REQUIREMENTS.md traceability row has matching VERIFICATION and SUMMARY evidence.

## Phase Verification Summary

| Phase | Status | Score | Notes |
|---|---|---|---|
| 01 — Device Ingest & Live Readout | passed | 8/8 | Re-verified after closing a flaky Realtime-test gap (commit `2b1d745`). MVP-mode format discrepancy flagged, non-blocking (no UI in this repo). |
| 02 — Automatic Risk Scoring & Status | passed | 6/6 | Re-verified after closing a failure-isolation test gap (commit `20fecb4`) and adding `02-SECURITY.md`. |
| 03 — Offline-Buffered Batch Sync | passed | 14/16 | 2 behavior-unverified items, both explicitly declared `human_judgment`/`backstop` tier in the plan (batch scoring call order — internal implementation detail, not externally observable goal). No test/artifact/link failures. |
| 04 — Historical Trends API | passed | 9/9 | Re-verified after closing a `npm run build` TS2345 type-check gap (commit `0d5ca52`). |

All 4 phases: **status: passed** in their VERIFICATION.md frontmatter. No phase is missing a VERIFICATION.md.

## Cross-Phase Integration & E2E Flows

Checked by `gsd-integration-checker`. Full source/migration trace across `src/app/api/ingest/route.ts`, `src/app/api/ingest/batch/route.ts`, `src/app/api/readings/route.ts`, `src/lib/risk/compute.ts`, `src/lib/risk/thresholds.ts`, and all 3 Supabase migrations.

**Wiring: 4/4 connected, 0 orphaned, 0 missing.**

1. **Live ingest → risk score → historical read** — WIRED. `POST /api/ingest` calls `computeAndPersistRiskScore` synchronously; `GET /api/readings` joins `risk_scores` via the `reading_id` FK. Covers ING-01, ING-02, STOR-01, STOR-02, RISK-01/02/03, READ-02, DEV-01.
2. **Batch/offline sync → risk score → backfill rescore → historical read** — WIRED. `POST /api/ingest/batch` reuses the identical `computeAndPersistRiskScore` function (no divergent scoring logic) and correctly rescans/rescoring windows affected by backfilled data. Covers ING-03, STOR-02, RISK-01/02/03, READ-02.
3. **Timestamp ordering consistency** — WIRED. Device-supplied epoch-ms `timestamp` (not upload time) is the sole ordering key across ingest, the 12h scoring window, and the historical read API's ordering.
4. **Schema consistency across all 3 migrations** — WIRED. FK/unique-constraint relationships and camelCase column naming are consistent everywhere; `npm run build` compiles with zero TypeScript errors.

**No broken flows found.**

**Requirements integration map:** all 11 requirements have at least one verified cross-phase touchpoint; see integration checker's full table (reproduced in tech debt below where it surfaced a warning).

**Note on READ-01/READ-02 Realtime consumer:** the actual subscriber-side wiring for Realtime lives in the external dashboard repo (frontend, built by teammates, out of scope for this backend branch) — cannot be verified from this repo; not treated as a gap since backend enablement (RLS + publication) is fully verified.

## Tech Debt (Non-Blocking — Review Before/At Close)

1. **`GET /api/readings` has no API-key/auth gate** (surfaced by integration checker). Protected only by the hardcoded `nb-001` device allow-list. Acceptable for v1's explicit single-device scope (DEV-01) but should be tracked as a backlog item before the route is exposed beyond a trusted network/demo.
2. **Two D-29 batch-scoring-order truths in Phase 3 remain human-judgment/backstop-tier**, unverified by a dedicated automated test (only the externally observable outcome — correct final risk status regardless of scoring order — is indirectly supported by other passing tests). Internal implementation detail, not the phase's externally observable goal.
3. **Phase 1's MVP-mode/user-story-format discrepancy** — carried forward twice as non-blocking; ROADMAP's own numbered success criteria are used instead.
4. **Nyquist validation never run** — all 4 phases' `VALIDATION.md` are `status: draft` (`wave_0_complete: false`). This is a coverage gap (validate-phase was never invoked), not a proven non-compliance. See below.

## Nyquist Compliance Discovery

| Phase | VALIDATION.md | Compliant | Action |
|---|---|---|---|
| 01 | exists (draft) | not-validated | `/gsd-validate-phase 01` |
| 02 | exists (draft) | not-validated | `/gsd-validate-phase 02` |
| 03 | exists (draft) | not-validated | `/gsd-validate-phase 03` |
| 04 | exists (draft) | not-validated | `/gsd-validate-phase 04` |

Discovery only — validate-phase was not auto-invoked.

## Verdict

**Status: tech_debt.** All 11 v1 requirements are satisfied, all 4 phases verified `passed`, all cross-phase integration and E2E flows are wired with zero broken flows. The milestone is functionally complete and ready to ship. The items above are accumulated debt worth a conscious decision (accept and backlog, or close first) — none block the v1.0 close.
