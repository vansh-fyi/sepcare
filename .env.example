# Server-side only — never expose to the browser bundle.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Client-safe — exposed to the browser bundle via the NEXT_PUBLIC_ prefix.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
